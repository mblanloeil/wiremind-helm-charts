consumers: # Define remoulade consumers to deploy
  # xxx define additional secrets to define
  enabled: true
  defaults:
    image:
      repository: xxx
      tag:  # Use default one by default
      pullPolicy: IfNotPresent # XXX
    command: remoulade
    resources:
      limits:
       cpu: "1"
       memory: 1500Mi
      requests:
       cpu: "0.1"
       memory: 128Mi
    #XXX remove and change and document
    #strategyType: RollingUpdate  # Can be Upgrade as well, see https://kubernetes.io/fr/docs/concepts/workloads/controllers/deployment/#strat%C3%A9gie
    ## updateStrategy for PostgreSQL StatefulSet and its slaves StatefulSets
    ## ref: https://kubernetes.io/docs/concepts/workloads/controllers/statefulset/#update-strategies
    updateStrategy:
      type: RollingUpdate
    expectedDeploymentScalePriority: 1  # See https://gitlab.cayzn.com/wiremind/devops/wiremind-kubernetes/-/blob/master/CustomResourceDefinition-expecteddeploymentscales.yaml#L47
    terminationGracePeriodSeconds: 180
    podAnnotations:
    affinity:
    nodeSelector: {}
  list:
    {}
    # export:
    #   normal:
    #     replicaCount: 2
    #     args: ["my", "arguments"]
    #     # disableMetrics: true  # By default, metrics are enabled, you can explicitely disable them
    #     autoscaling:
    #       enabled: true
    #       # minReplicas SHOULD be >= 1 for now.
    #       minReplicas: 1
    #       maxReplicas: 1
    #       describedService: ""
    #       metricName: ""
    #       metricMatchLabels:
    #         queue: queue_name
    #       targetMetricValue: ""
    #     expectedDeploymentScalePriority: 0
    #     usePrivilegedWiremindConf: false  (default value is false, if true, uses wiremind conf with privileged postgresql user)

superbowl:
  enabled: true

  replicaCount: 1

  image:
    repository: nginx
    pullPolicy: IfNotPresent
    # Overrides the image tag whose default is the chart appVersion.
    tag: ""
  frontend:
    image:
      repository: ghcr.io/wiremind/super-bowl
      pullPolicy: Always # IfNotPresent
      # Overrides the image tag whose default is the chart appVersion.
      tag: ""
    resources: {}
      # We usually recommend not to specify default resources and to leave this as a conscious
      # choice for the user. This also increases chances charts run on environments with little
      # resources, such as Minikube. If you do want to specify resources, uncomment the following
      # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
      # limits:
      #   cpu: 100m
      #   memory: 128Mi
      # requests:
      #   cpu: 100m
      #   memory: 128Mi
  backend:
    # XXX document here what to do
    image:
      repository: xxx
      pullPolicy: IfNotPresent
      tag: xxx
      command:
      args:
    resources: {}
      # We usually recommend not to specify default resources and to leave this as a conscious
      # choice for the user. This also increases chances charts run on environments with little
      # resources, such as Minikube. If you do want to specify resources, uncomment the following
      # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
      # limits:
      #   cpu: 100m
      #   memory: 128Mi
      # requests:
      #   cpu: 100m
      #   memory: 128Mi

  imagePullSecrets: []

  podAnnotations: {}

  podSecurityContext: {}
    # fsGroup: 2000

  securityContext: {}
    # capabilities:
    #   drop:
    #   - ALL
    # readOnlyRootFilesystem: true
    # runAsNonRoot: true
    # runAsUser: 1000

  service:
    type: ClusterIP
    port: 80

  ingress:
    enabled: false
    className: ""
    annotations: {}
      # kubernetes.io/ingress.class: nginx
      # kubernetes.io/tls-acme: "true"
    hosts:
      - host: chart-example.local
        paths:
          - path: /
            pathType: ImplementationSpecific
    tls: []
    #  - secretName: chart-example-tls
    #    hosts:
    #      - chart-example.local

  autoscaling:
    enabled: false
    minReplicas: 1
    maxReplicas: 100
    targetCPUUtilizationPercentage: 80
    # targetMemoryUtilizationPercentage: 80

  nodeSelector: {}

  tolerations: []

  affinity: {}

# XXX generate env for remoulade to connect to pg / etc

# XXX create tests in ci dir
